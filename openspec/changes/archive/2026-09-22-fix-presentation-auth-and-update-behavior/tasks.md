## 1. 展示生命周期

- [x] 1.1 调整显示模式和目标屏幕订阅，使协调器在 store 完成赋值后统一 reconcile，并用回归测试验证单次模式切换立即生效
- [x] 1.2 为悬浮岛窗口增加展示意图状态，在解锁恢复前检查该状态，并用测试验证 Menu Bar 模式解锁不显示窗口、Notch 模式正常恢复

## 2. Claude 限流恢复

- [x] 2.1 提取常规刷新与重新认证共用的 Claude 429 冷却状态转换，并用测试验证冷却截止时间、限流状态和单次延迟重试
- [x] 2.2 让凭据监听和重新认证轮询在冷却期内停止网络探测，并用测试验证新凭据遇到 429 后不会继续五秒重试且不会保留过期登录提示

## 3. Sparkle 更新源配置

- [x] 3.1 修改构建脚本以区分 `SU_FEED_URL` 未设置、显式为空和自定义 URL，并用脚本测试验证三种生成结果
- [x] 3.2 让更新控制器和设置界面安全处理无 feed 构建，并验证空 feed 启动时不发起检查、标准构建仍使用原生产 URL 和公钥

## 4. 本地成本刷新

- [x] 4.1 为用量账本增加线程安全的 source 扫描状态查询，并用账本测试验证无记录、已有记录及读取失败时的行为
- [x] 4.2 将 OpenCode 刷新改为首次全量回填、后续 30 天重叠增量扫描，并用测试验证旧日志轮转后账本历史仍参与汇总
- [x] 4.3 重排成本刷新任务，使 Claude/Codex 自有日志与共享 OpenCode 扫描并发启动且共享扫描只执行一次，并用可控任务测试验证两类结果正确合并和独立提交

## 5. 集成验证

- [x] 5.1 运行 `bash scripts/run-tests.sh`，确认全部回归测试通过
- [x] 5.2 运行 `SU_FEED_URL= ./build.sh` 和标准 `./build.sh` 的配置检查或等价构建验证，确认无 feed 与生产 feed 产物符合规格且 Sparkle 公钥、bundle ID、版本字段未变化
- [x] 5.3 运行 `bash scripts/verify.sh`，确认通用应用完成构建并能无崩溃启动
