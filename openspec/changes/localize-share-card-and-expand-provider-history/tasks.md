## 1. 分享卡片本地化

- [x] 1.1 将分享卡片工作室、卡片画布、里程碑文案及无障碍文字统一接入应用级本地化资源，并补齐简体中文文案；静态检查卡片可见字符串和复制/分享文案，确认中文语言路径不残留英文描述。
- [x] 1.2 让日期、数量、货币和分享摘要使用当前应用语言的区域格式；检查中文与英文路径分别生成对应格式，品牌名和网址保持原样。
- [x] 1.3 检查动态标题、提供商图例和三种卡片比例在中文内容下的布局，确保长文案仍可完整阅读。

## 2. 全提供商本地历史

- [x] 2.1 识别 OpenCode 中明确标记为 DeepSeek 的会话，并把 DeepSeek 纳入 Overview 与全历史卡片的本地每日汇总；静态追踪记录从读取、归属到汇总的链路，确认不读取或转换钱包余额。
- [x] 2.2 为新增的 DeepSeek 归属识别加入一次性全历史回填标记，仅在完整扫描和账本保存成功后完成回填；检查失败时会重试全量扫描，成功后恢复 30 天增量扫描。
- [x] 2.3 让热力图的提供商筛选、单日明细和汇总布局容纳所有有记录的提供商；静态检查没有用量的提供商不会伪造 token 数或显示为有活动。

## 3. OpenSpec 校验

- [x] 3.1 运行 `openspec validate localize-share-card-and-expand-provider-history --strict --type change`，确认提案、规格、设计和任务结构有效。
